#+ name: Genid-nodes-per-property
SELECT ?property (COUNT(DISTINCT ?node) AS ?aantal) 
WHERE {
  GRAPH ?g {
    ?node ?property ?o .
    FILTER(STRSTARTS(STR(?node), "https://linkeddata.cultureelerfgoed.nl/.well-known/genid/"))
  }
}
GROUP BY ?property
ORDER BY DESC(?aantal)
